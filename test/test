import sys

N = int(input())
s = input()

rec = []
ans = 0
# left = 0
right = 0
while right < len(s):
    if s[right] != rec[-1] or not rec:
        rec.append(s[right])
   
    while s[right] == rec[-1]:
        rec = rec[:-1]
        
print(ans)